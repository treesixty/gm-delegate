{
  "name": "gm-delegate",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Foundry VTT v14 module + local agent server. The AI prompts; the GM speaks.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint scripts tests"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "eslint": "^9.9.0",
    "vitest": "^2.0.5"
  }
}
